{
  "name": "odroe_fs_deployment_workspace",
  "private": true,
  "engines": {
    "node": ">=12.0",
    "pnpm": ">=6.0"
  },
  "scripts": {
    "format": "prettier --write .",
    "build": "npm run build -ws",
    "publish": "npm publish -ws",
    "version": "npm version -ws --no-commit-hooks --no-git-tag-version"
  },
  "devDependencies": {
    "@types/node": "^17.0.41",
    "prettier": "^2.6.2",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.3"
  },
  "dependencies": {
    "rimraf": "^3.0.2"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ]
}
