{
  "private": true,
  "name": "0.using_fs_adapter",
  "scripts": {
    "prestart": "npm run build",
    "start": "node dist/index.js",
    "serve": "ts-node src/index.ts",
    "build": "tsc --build"
  },
  "devDependencies": {
    "ts-node": "^10.7.0"
  },
  "dependencies": {
    "@odroe/fs": "^0.1.3",
    "@odroe/fs-local": "^0.1.3"
  },
  "version": "0.1.7"
}
